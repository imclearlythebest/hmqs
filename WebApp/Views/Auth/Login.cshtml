@model LoginDto
@{
  ViewData["Title"] = "Login";
}

@section Styles {
  <style>
    .auth-wrap {
      max-width: 520px;
      margin: 1.5rem auto;
    }
  </style>
}

<div class="auth-wrap hmqs-panel p-4">
  <h4 class="mb-3">Login</h4>
  <form asp-action="Login" asp-controller="Auth" method="post" class="row g-3">
    <div class="col-12">
      <label for="username" class="form-label">Username</label>
      <input type="text" id="username" name="username" class="form-control" required>
    </div>
    <div class="col-12">
      <label for="password" class="form-label">Password</label>
      <input type="password" id="password" name="password" class="form-control" required>
    </div>
    <div class="col-12 d-flex justify-content-between align-items-center">
      <button type="submit" class="btn btn-primary">Login</button>
      <a asp-controller="Auth" asp-action="Register" class="btn btn-link">Create account</a>
    </div>
    <div class="col-12 text-danger" asp-validation-summary="All"></div>
  </form>
</div>